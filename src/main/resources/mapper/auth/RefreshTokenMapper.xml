<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dailytable.dailytable.domain.auth.RefreshTokenMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO refresh_tokens (user_id, token, expires_at)
        VALUES (#{userId}, #{token}, #{expiresAt})
    </insert>

    <delete id="deleteByToken">
        DELETE FROM refresh_tokens WHERE token = #{token}
    </delete>

    <select id="selectByTokenAndNotExpired" resultType="com.dailytable.dailytable.domain.auth.dto.entity.RefreshToken">
        SELECT id, user_id AS userId, token, expires_at AS expiresAt
        FROM refresh_tokens
        WHERE token = #{token} AND expires_at &gt; #{now}
    </select>
</mapper>
